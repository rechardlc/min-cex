# gateway.api 做什么用

这是 `go-zero` API 服务的接口定义文件。

## 作用

- 定义请求和响应结构
- 定义路由
- 定义 handler 名称

## 你学习时要重点看什么

- `type` 是怎么定义参数结构的
- `service` 里如何绑定路由
- `@handler` 和 `internal/handler` 的关系

## 当前示例为什么只放健康检查

因为健康检查是最简单、最适合入门的接口。先搞懂最小路径，再扩展登录、用户、订单等接口。

